{
  "name": "solana-coin-flip-monorepo",
  "version": "0.2.0",
  "private": true,
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build",
    "start": "cd app && npm start",
    "lint": "cd app && npm run lint"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Salilgbc/coin-flip-solana.git"
  }
}
